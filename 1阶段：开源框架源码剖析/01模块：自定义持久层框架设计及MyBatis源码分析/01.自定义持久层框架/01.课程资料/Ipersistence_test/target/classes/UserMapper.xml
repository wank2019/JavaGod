<!-- namespace：当前UserMapper.xml的唯一标识-->
<mapper namespace="user">

    <!--查询所有-->
    <!-- id：当前select标签的唯一标识-->
    <!--想要精准定位到当前这条SQL语句：是通过 namespace和该条select标签中的id来组成一个statementId来进行定位的 ， 如该条SQL的statementId为 user.selectList-->
    <!--resultType：该条SQL语句返回结果的返回值类型-->
    <select id="selectList" resultType="com.eayon.pojo.User">
        select * from user
    </select>

    <!--条件查询-->
    <!--paramterType：参数类型-->
    <select id="selectOne" resultType="com.eayon.pojo.User"  paramterType="com.eayon.pojo.User">
        select * from user where id = #{id} and username = #{username}
    </select>

</mapper>